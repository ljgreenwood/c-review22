#include <stdlib.h>
#include <math.h>

// prototypes

void draw_circle(void);

void draw_intersect(void);

void draw_base(void);

void draw_triangle(void);

// ftype fname(void) --> type returned, function name, arguments passed in, 

/*

function interface comment -->  precondition: assumed true before function call
                                postcondition: assumed true after function call

    /
    *   Computes circumference of a circle with radius r
    *   Pre: r is defined and is > 0
    *       PI si a constant macro representing an approximation of pi
    *       library Math.h is included
    *       etc.
    /

        **types of formal parameter declaration list must match with actual arguments passed in main()

ftype
fname(formal parameter declaration list --> actual arguments will be entered here in main){
    local variable declarations
    executables
}

*/

int main(){

   // circum = find_circum(radius); 










    // int value = -1;
    // int updated = 0;
    // updated = abs(value);
    /*
    Functions:
        abs(x) --> 
        log(x) --> natural logarithm
        log10(x) --> log with base 10
        ceil(x) --> rounds to nearest whole num
        fabs(x) --> for type double
        floor(x)
        sin(x) 
        sqrt(x) --> returns non-negative square root
    */

    return(0);
}

void draw_circle(void){

    printf("  *  \n");
    printf("*   *\n");
    printf(" * *\n");
    // doesn't need a return statement
}

void draw_triangle (void){

    draw_intersect();
    draw_base();

}